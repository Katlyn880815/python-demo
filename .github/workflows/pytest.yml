name: pytest
on: workflow_dispatch

jobs:
  pytest:
    runs-on: ubuntu-latest
    steps:
      - name: clone repository from GitHub
        run: |
          git clone https://github.com/Katlyn880815/python-demo.git
          cd python-demo
      - name: install pytest
        run: pip install pytest
      - name: run pytest
        run: pytest
